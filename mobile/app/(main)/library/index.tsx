import { Pressable,StyleSheet,View } from 'react-native'; import { router } from 'expo-router'; import { Ionicons } from '@expo/vector-icons'; import { Screen } from '@/components/ui/Screen'; import { Text } from '@/components/ui/Text'; import { Card } from '@/components/ui/Card'; import { useTheme } from '@/theme/useTheme';
const items=[['Notes','notes-outline','/(main)/notes'],['Documents','document-text-outline','/(main)/documents'],['Research','earth-outline','/(main)/research'],['Flashcards','albums-outline','/(main)/flashcards'],['Quizzes','help-circle-outline','/(main)/quiz'],['Study plans','calendar-outline','/(main)/study-plan'],['Formulas','calculator-outline','/(main)/formulas'],['PYQs','archive-outline','/(main)/pyq']] as const;
export default function Library(){const c=useTheme();return <Screen><Text variant="display">Library</Text><Text muted>Everything you have saved, generated, or imported.</Text><View style={s.grid}>{items.map(([label,icon,href])=><Pressable key={label} onPress={()=>router.push(href)} style={s.item}><Card style={{minHeight:116}}><Ionicons name={icon} size={26} color={c.primary}/><Text variant="subtitle">{label}</Text></Card></Pressable>)}</View></Screen>}
const s=StyleSheet.create({grid:{flexDirection:'row',flexWrap:'wrap',gap:12},item:{width:'48%',flexGrow:1}});
