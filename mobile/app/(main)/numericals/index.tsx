import { FeatureScreen } from '@/features/feature/FeatureScreen';
export default function Page(){return <FeatureScreen title='Numerical solver' description='Given → required → formula → substitution → calculation → unit → verification.' kind='numericals' inputLabel='Problem'/>}
