import { useState } from 'react'; import { Alert,Pressable,View } from 'react-native'; import { Link } from 'expo-router'; import { Screen } from '@/components/ui/Screen'; import { Text } from '@/components/ui/Text'; import { Field } from '@/components/ui/Field'; import { Button } from '@/components/ui/Button'; import { useAuth } from '@/features/auth/AuthProvider'; import { GoogleSignInButton } from '@/features/auth/GoogleSignInButton';
export default function Signup(){const {signup}=useAuth();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);async function submit(){if(name.trim().length<2||!email.includes('@')||password.length<10)return Alert.alert('Check your details','Add your name, a valid email, and a password of at least 10 characters.');setBusy(true);try{await signup(name.trim(),email.trim().toLowerCase(),password)}catch(e){Alert.alert('Could not create account',e instanceof Error?e.message:'Try again.')}finally{setBusy(false)}}return <Screen><View style={{gap:8,marginTop:24}}><Text variant="display">Start learning</Text><Text muted>Your study workspace, tuned to you.</Text></View><Field label="Name" value={name} onChangeText={setName} autoComplete="name"/><Field label="Email" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none"/><Field label="Password" value={password} onChangeText={setPassword} secureTextEntry/><Button label="Create account" onPress={submit} loading={busy}/><GoogleSignInButton label="Sign up with Google"/><Link href="/(auth)/login" asChild><Pressable style={{minHeight:44,justifyContent:'center',alignItems:'center'}}><Text>Already have an account?</Text></Pressable></Link></Screen>}
