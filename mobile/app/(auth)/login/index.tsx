import { useState } from 'react'; import { Alert,Pressable,View } from 'react-native'; import { Link } from 'expo-router'; import { Screen } from '@/components/ui/Screen'; import { Text } from '@/components/ui/Text'; import { Field } from '@/components/ui/Field'; import { Button } from '@/components/ui/Button'; import { useAuth } from '@/features/auth/AuthProvider'; import { GoogleSignInButton } from '@/features/auth/GoogleSignInButton'; import { useTheme } from '@/theme/useTheme';
export default function Login(){const c=useTheme();const {login}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);async function submit(){if(!email.includes('@')||password.length<8)return Alert.alert('Check your details','Use a valid email and a password of at least 8 characters.');setBusy(true);try{await login(email.trim().toLowerCase(),password)}catch(e){Alert.alert('Sign in failed',e instanceof Error?e.message:'Try again.')}finally{setBusy(false)}}return <Screen contentStyle={{justifyContent:'center',minHeight:'100%'}}><View style={{gap:8}}><Text variant="display">Welcome back</Text><Text muted>Pick up where you left off.</Text></View><Field label="Email" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none" autoComplete="email"/><Field label="Password" value={password} onChangeText={setPassword} secureTextEntry autoComplete="current-password"/><Link href="/(auth)/forgot-password" asChild><Pressable style={{minHeight:44,justifyContent:'center'}}><Text style={{color:c.primary,fontWeight:'700'}}>Forgot password?</Text></Pressable></Link><Button label="Sign in" onPress={submit} loading={busy}/><GoogleSignInButton label="Continue with Google"/><Link href="/(auth)/signup" asChild><Pressable style={{minHeight:44,justifyContent:'center',alignItems:'center'}}><Text>Create an account</Text></Pressable></Link></Screen>}
