import { useState } from 'react'; import { Alert } from 'react-native'; import { router } from 'expo-router'; import { Screen } from '@/components/ui/Screen'; import { Text } from '@/components/ui/Text'; import { Field } from '@/components/ui/Field'; import { Button } from '@/components/ui/Button'; import { api } from '@/services/api/client';
export default function Forgot(){const [email,setEmail]=useState('');const [busy,setBusy]=useState(false);async function submit(){setBusy(true);try{await api.post('/auth/forgot-password',{email});Alert.alert('Check your email','If that account exists, a reset link is on the way.',[{text:'OK',onPress:()=>router.back()}])}finally{setBusy(false)}}return <Screen contentStyle={{justifyContent:'center',minHeight:'100%'}}><Text variant="display">Reset password</Text><Text muted>We will send a short-lived, single-use reset link.</Text><Field label="Email" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none"/><Button label="Send reset link" onPress={submit} loading={busy}/><Button label="Back" variant="ghost" onPress={()=>router.back()}/></Screen>}
