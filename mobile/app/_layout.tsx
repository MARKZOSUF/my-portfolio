import 'react-native-reanimated'; import { Stack } from 'expo-router'; import { StatusBar } from 'expo-status-bar'; import { QueryClient,QueryClientProvider } from '@tanstack/react-query'; import { GestureHandlerRootView } from 'react-native-gesture-handler'; import { AuthProvider } from '@/features/auth/AuthProvider'; import { OfflineBanner } from '@/components/ui/OfflineBanner';
const client=new QueryClient({defaultOptions:{queries:{retry:2,networkMode:'offlineFirst'},mutations:{retry:1,networkMode:'offlineFirst'}}});
export default function Root(){return <GestureHandlerRootView style={{flex:1}}><QueryClientProvider client={client}><AuthProvider><StatusBar style="auto"/><OfflineBanner/><Stack screenOptions={{headerShown:false,animation:'slide_from_right'}}><Stack.Screen name="index"/><Stack.Screen name="(auth)"/><Stack.Screen name="(main)"/><Stack.Screen name="create-modal" options={{presentation:'transparentModal',animation:'fade'}}/></Stack></AuthProvider></QueryClientProvider></GestureHandlerRootView>}
