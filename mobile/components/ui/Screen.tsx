import type { PropsWithChildren } from 'react'; import { KeyboardAvoidingView, Platform, ScrollView, StyleSheet, ViewStyle } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { useTheme } from '@/theme/useTheme';
export function Screen({children,scroll=true,contentStyle}:{children:React.ReactNode;scroll?:boolean;contentStyle?:ViewStyle}){const c=useTheme();const body=scroll?<ScrollView keyboardShouldPersistTaps="handled" contentContainerStyle={[s.content,contentStyle]}>{children}</ScrollView>:<KeyboardAvoidingView behavior={Platform.OS==='ios'?'padding':undefined} style={[s.content,{flex:1},contentStyle]}>{children}</KeyboardAvoidingView>;return <SafeAreaView edges={['top','left','right']} style={[s.safe,{backgroundColor:c.canvas}]}>{body}</SafeAreaView>}
const s=StyleSheet.create({safe:{flex:1},content:{padding:16,paddingBottom:36,gap:16}});
