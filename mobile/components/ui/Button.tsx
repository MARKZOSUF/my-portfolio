import type { ReactNode } from 'react'; import { ActivityIndicator, Pressable, StyleSheet, View } from 'react-native'; import * as Haptics from 'expo-haptics'; import { Text } from './Text'; import { useTheme } from '@/theme/useTheme';
export function Button({label,onPress,variant='primary',loading=false,disabled=false,icon}:{label:string;onPress:()=>void|Promise<void>;variant?:'primary'|'secondary'|'danger'|'ghost';loading?:boolean;disabled?:boolean;icon?:ReactNode}){const c=useTheme();const bg=variant==='primary'?c.primary:variant==='danger'?c.danger:variant==='ghost'?'transparent':c.surface;const fg=variant==='primary'||variant==='danger'?'#fff':c.text;return <Pressable accessibilityRole="button" accessibilityLabel={label} disabled={disabled||loading} onPress={async()=>{await Haptics.selectionAsync();await onPress()}} style={({pressed})=>[s.base,{backgroundColor:bg,borderColor:variant==='secondary'?c.border:bg,opacity:disabled?.45:pressed?.78:1}]}>{loading?<ActivityIndicator color={fg}/>:<View style={s.row}>{icon}<Text style={{color:fg,fontWeight:'700'}}>{label}</Text></View>}</Pressable>}
const s=StyleSheet.create({base:{minHeight:48,paddingHorizontal:18,borderRadius:12,borderWidth:1,alignItems:'center',justifyContent:'center'},row:{flexDirection:'row',gap:8,alignItems:'center'}});
