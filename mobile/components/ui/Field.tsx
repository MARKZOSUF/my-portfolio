import { TextInput, StyleSheet, View } from 'react-native'; import type { TextInputProps } from 'react-native'; import { Text } from './Text'; import { useTheme } from '@/theme/useTheme';
export function Field({label,error,...props}:TextInputProps&{label:string;error?:string}){const c=useTheme();return <View style={{gap:7}}><Text variant="caption" style={{fontWeight:'700'}}>{label}</Text><TextInput accessibilityLabel={label} placeholderTextColor={c.muted} style={[s.input,{color:c.text,backgroundColor:c.surface,borderColor:error?c.danger:c.border},props.multiline&&s.multi]} {...props}/>{error&&<Text variant="caption" style={{color:c.danger}}>{error}</Text>}</View>}
const s=StyleSheet.create({input:{minHeight:48,borderWidth:1,borderRadius:12,paddingHorizontal:14,fontSize:16},multi:{minHeight:112,paddingTop:12,textAlignVertical:'top'}});
