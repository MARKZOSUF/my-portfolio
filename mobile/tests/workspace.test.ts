import { useWorkspaceStore } from '@/store/workspace'; describe('workspace state',()=>{beforeEach(()=>useWorkspaceStore.getState().reset());it('tracks generation lifecycle',()=>{useWorkspaceStore.getState().setTopic('Kirchhoff');useWorkspaceStore.getState().start();expect(useWorkspaceStore.getState().status).toBe('generating');useWorkspaceStore.getState().updateProgress(.5);expect(useWorkspaceStore.getState().progress).toBe(.5)})});
