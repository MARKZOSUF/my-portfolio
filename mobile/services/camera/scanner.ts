import * as ImagePicker from 'expo-image-picker'; import * as ImageManipulator from 'expo-image-manipulator';
export async function scanPage(){const permission=await ImagePicker.requestCameraPermissionsAsync();if(!permission.granted)throw new Error('Camera permission is required');const result=await ImagePicker.launchCameraAsync({mediaTypes:['images'],quality:1,allowsEditing:true});if(result.canceled)return;const asset=result.assets[0];if(!asset)return;return ImageManipulator.manipulateAsync(asset.uri,[{resize:{width:1800}}],{compress:.86,format:ImageManipulator.SaveFormat.JPEG});}
