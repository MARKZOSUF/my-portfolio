"""Exam-ready definitions, persisted as StudyArtifact(kind='definitions')."""
from app.knowledge.artifacts import build_artifact_router
router=build_artifact_router('definitions','/definitions','definitions')
