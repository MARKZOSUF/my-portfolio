export async function sendMail(env,{to,subject,html,text}){if(!env.RESEND_API_KEY||!env.EMAIL_FROM)return{sent:false,reason:"Email provider is not configured."};const r=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${env.RESEND_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({from:env.EMAIL_FROM,to:[to],subject,html,text})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||"Email delivery failed.");return{sent:true,id:d.id}}
