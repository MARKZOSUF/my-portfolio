from alembic import op
from database.migrations.helpers import create,drop
revision='0005_learning';down_revision='0004_research';branch_labels=None;depends_on=None
R=revision
TABLES=[('questions', 'CREATE TABLE IF NOT EXISTS questions(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,topic varchar(220) NOT NULL,type varchar(40) NOT NULL,prompt text NOT NULL,options jsonb,answer text NOT NULL,explanation text NOT NULL,difficulty varchar(16) NOT NULL,source_id uuid REFERENCES sources(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now())'), ('previous_year_questions', 'CREATE TABLE IF NOT EXISTS previous_year_questions(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,exam varchar(180) NOT NULL,year integer NOT NULL,topic varchar(220) NOT NULL,prompt text NOT NULL,marks double precision,source_id uuid NOT NULL REFERENCES sources(id) ON DELETE RESTRICT,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now())'), ('exams', "CREATE TABLE IF NOT EXISTS exams(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,name varchar(200) NOT NULL,exam_date date NOT NULL,syllabus jsonb NOT NULL DEFAULT '{}',total_marks integer,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now())"), ('quiz_attempts', "CREATE TABLE IF NOT EXISTS quiz_attempts(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,topic varchar(220) NOT NULL,score integer NOT NULL,total integer NOT NULL,duration_seconds integer,answers jsonb NOT NULL DEFAULT '[]',created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now())"), ('flashcards', 'CREATE TABLE IF NOT EXISTS flashcards(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,topic varchar(220) NOT NULL,front text NOT NULL,back text NOT NULL,due_at timestamptz NOT NULL DEFAULT now(),interval_days integer NOT NULL DEFAULT 0,ease double precision NOT NULL DEFAULT 2.5,repetitions integer NOT NULL DEFAULT 0,bookmarked boolean NOT NULL DEFAULT false,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now())'), ('flashcard_reviews', 'CREATE TABLE IF NOT EXISTS flashcard_reviews(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),flashcard_id uuid NOT NULL REFERENCES flashcards(id) ON DELETE CASCADE,owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,quality integer NOT NULL,reviewed_at timestamptz NOT NULL DEFAULT now(),previous_interval integer NOT NULL,next_interval integer NOT NULL)'), ('study_plans', "CREATE TABLE IF NOT EXISTS study_plans(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,name varchar(220) NOT NULL,exam_date date NOT NULL,daily_minutes integer NOT NULL,strategy jsonb NOT NULL DEFAULT '{}',created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now())"), ('study_tasks', "CREATE TABLE IF NOT EXISTS study_tasks(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),plan_id uuid NOT NULL REFERENCES study_plans(id) ON DELETE CASCADE,owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,title varchar(255) NOT NULL,scheduled_for date NOT NULL,minutes integer NOT NULL,kind varchar(40) NOT NULL,status varchar(32) NOT NULL DEFAULT 'planned',version integer NOT NULL DEFAULT 1,postponed_from date,completed_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now())"), ('progress', "CREATE TABLE IF NOT EXISTS progress(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,topic varchar(220) NOT NULL,mastery double precision NOT NULL DEFAULT 0,confidence double precision NOT NULL DEFAULT 0,attempt_count integer NOT NULL DEFAULT 0,correct_count integer NOT NULL DEFAULT 0,mistake_count integer NOT NULL DEFAULT 0,study_minutes integer NOT NULL DEFAULT 0,last_reviewed_at timestamptz,next_review_at timestamptz,mistakes jsonb NOT NULL DEFAULT '[]',created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(owner_id,topic))"), ('study_task_history', 'CREATE TABLE IF NOT EXISTS study_task_history(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),task_id uuid NOT NULL REFERENCES study_tasks(id) ON DELETE CASCADE,owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,from_status varchar(32) NOT NULL,to_status varchar(32) NOT NULL,from_date date,to_date date,reason varchar(255),created_at timestamptz NOT NULL DEFAULT now())'), ('study_activities', "CREATE TABLE IF NOT EXISTS study_activities(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),owner_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,kind varchar(40) NOT NULL,topic varchar(220),minutes integer NOT NULL DEFAULT 0,score double precision,occurred_at timestamptz NOT NULL DEFAULT now(),metadata jsonb NOT NULL DEFAULT '{}')")]
def upgrade():
 op.execute('CREATE EXTENSION IF NOT EXISTS pgcrypto');op.execute('CREATE EXTENSION IF NOT EXISTS vector')
 for name,ddl in TABLES:create(R,name,ddl)
def downgrade():
 for name,_ in reversed(TABLES):drop(R,name)
