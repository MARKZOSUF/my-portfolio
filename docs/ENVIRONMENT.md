# Environment

Use `.env.example` as the complete grouped reference. Production startup rejects development AI/embedding/email modes, local storage, disabled malware scanning, wildcard CORS, and weak secrets. Provider keys remain server-side. Never commit `.env`.
