# Mobile build

Install locked dependencies in `mobile`, run `npm run typecheck`, `npm run lint`, and `npm test`, then use Expo/EAS profiles. Android deep links include password reset and email verification. This sandbox did not contain Expo/Jest dependencies or an Android SDK/device, so runtime validation is explicitly not executed.
