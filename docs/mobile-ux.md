# Mobile UX

All primary routes include actionable inputs and server calls. Offline banner is explicit, mutations queue/retry, and research/ingestion show real job state. Remaining: encrypted per-user cache, conflict merge UI, TalkBack/VoiceOver and dynamic-type device verification.
