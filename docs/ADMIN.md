# Administration

Administrative capability must be enforced using the persisted `is_admin` flag on every server route. Audit events intentionally omit tokens, passwords, API keys, and note contents. This package retains the secure model foundation; a complete admin console remains partially implemented and is not claimed as production-ready.
