# Testing

Run `./scripts/check.sh`, `python -m compileall backend/app database/migrations`, `pytest -q`, `npm --prefix mobile run typecheck`, `npm --prefix mobile test -- --runInBand`, JSON/YAML parsing, `docker compose config`, Alembic round trip, provider contracts, then device and accessibility matrices.
